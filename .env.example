# Render (без Shell): Build = npm install && npm run prisma:generate && npm run build
# Start = npm run render:start   (внутри: prisma migrate deploy + node dist/main)
#
# Render Postgres: Internal/External URL из панели (?schema=public&sslmode=require при необходимости)
DATABASE_URL="postgresql://postgres:matveeva8812@localhost:5432/trello-db?schema=public"
JWT_SECRET="SAKHA-SIRE"

# Разрешённые origins для CORS (через запятую). Пример для продакшена:
# CORS_ORIGINS=http://localhost:3000,https://your-app.vercel.app
CORS_ORIGINS=http://localhost:3000
